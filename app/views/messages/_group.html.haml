.left-content__group-content
  .left-content__group-name
    - current_user.groups.each do |group|
      %p
        = link_to group.name, group_messages_path(group), method: :get
        .left-content__group-text
          %p 回せ
